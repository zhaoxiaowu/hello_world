springboot  微服务


Spring 的组件代码是轻量级的，但它的配置却是重量级的。
项目的依赖管理  库的版本冲突。

Spring Boot  使开发者可以更容易构建基于spring的应用程序和服务。 
提供 固定  约定由于配置风格的框架

特点：使spring开发更加迅速

1.开箱即用，无需xml配置。同时可以修改默认值满足。
2.提供了一些大型项目中常见的非功能特性。

整合了一些子项目

热部署

springboot可以创建独立运行的应用而不依赖容器   


提供maven的极简配置  缺点：引入不需要的包

jackson的基本演绎法
@JsonIgnore
@JsonFormat(pattern = "yyyy-MM-dd hh:mm:ss a",locale = "zh",timezone = "GMT+8")
 

